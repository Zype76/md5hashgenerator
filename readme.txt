The purpose of this project is to create a script that automaticall generates md5 hashes.

Planned features:

-An automated system that continually generates hashes for ever text/number combination possible.
-A system that adds the generated values to a database.
-A program that allows you to query the hashed values by entering an unsalted MD5 hash or text string.
